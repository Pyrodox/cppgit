#include <iostream>

int main()
{
    const std::string exclam = "!";
    //const std::string message = "Hello" + ", world" + exclam;

    return 0;
}
//No, because the message is not a number type and it cannot cocatenate strings that don't have at least a variable in its cocatenation.